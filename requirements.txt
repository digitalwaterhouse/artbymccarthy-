Flask==3.1.2
Pillow==12.0.0
fonttools==4.64.0
gunicorn==21.2.0
# QR labels for individual works. Generated on the server — no third-party
# service, nothing phoning home, and the code keeps working offline.
qrcode==8.2

# Stripe Checkout. It was missing entirely, so `import stripe` failed on the
# server, payments.enabled() was False and every Purchase button quietly fell
# back to the enquiry form -- with the keys set or not. Only the signature
# check on the webhook actually needs this library.
stripe==15.6.1

# Claude, for the "find calls near you" search on the Opportunities page. The
# calls-for-entry world publishes no feed, so the only way to discover a call
# is to read the open web -- this is the server-side web search tool. With no
# ANTHROPIC_API_KEY set, finder.enabled() is False and that panel says so;
# nothing else on the site depends on it.
anthropic==1.5.0
